% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{compute_AreaCDF}
\alias{compute_AreaCDF}
\title{Area between CDFs (AreaCDF) for two groups}
\usage{
compute_AreaCDF(
  data,
  target_col,
  group_col,
  n = NULL,
  weights_col = NULL,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame.}

\item{target_col}{Character; numeric target column.}

\item{group_col}{Character; binary group column (two groups; values can be any two labels).}

\item{n}{Integer or NULL; grid size. If NULL, uses exact knots (union of unique values).}

\item{weights_col}{Optional character; column name of nonnegative weights.}

\item{verbose}{Logical; print small diagnostics.}
}
\value{
A list with \code{AreaCDF}, the evaluation \code{grid}, and the two CDF
vectors on that grid (named by the original group labels).
}
\description{
Computes the L1 area between two empirical CDFs. In 1D this equals the
first Wasserstein distance W1. Supports optional per-row weights.
}
\examples{
set.seed(1)
df <- data.frame(score = c(rnorm(150,0), rnorm(150,0.5)),
                 grp   = rep(c(0,1), each = 150))
out <- compute_AreaCDF(df, "score", "grp", n = NULL)
out$AreaCDF
}
