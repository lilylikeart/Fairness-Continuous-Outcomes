% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{compute_AreaPDF_cond}
\alias{compute_AreaPDF_cond}
\title{Compute Conditional AreaPDF Fairness Metric}
\usage{
compute_AreaPDF_cond(
  data,
  target_col,
  condition_col,
  group_col,
  bwAdj = 1,
  method = c("hist", "boxplot"),
  num_bins = 10,
  min_per_group = 2,
  n = 1000,
  verbose = FALSE
)
}
\arguments{
\item{data}{A data frame containing the observations.}

\item{target_col}{Name of the numeric outcome (score) column.}

\item{condition_col}{Name of the conditioning variable used for binning.}

\item{group_col}{Name of the binary group column (0/1).}

\item{bwAdj}{Bandwidth adjustment passed to \code{compute_AreaPDF()}.}

\item{method}{Method used to construct bins. Either \code{"hist"} or
\code{"boxplot"}.}

\item{num_bins}{Number of bins when \code{method = "hist"}.}

\item{min_per_group}{Minimum number of observations required in each
group within a bin to compute the metric.}

\item{n}{Number of grid points used for density estimation.}

\item{verbose}{Logical; if \code{TRUE}, progress information is printed.}
}
\value{
A list containing:
\describe{
\item{mean_AreaPDF}{Mean AreaPDF across valid bins.}
\item{breaks}{Bin boundaries.}
\item{per_bin}{Summary statistics for each bin.}
\item{pointwise}{Pointwise Hellinger distance and densities.}
\item{used_data}{Input data annotated with bin assignments.}
\item{groups}{Group labels.}
}
}
\description{
Computes the AreaPDF fairness metric within bins of a conditioning
variable and summarizes the fairness across all bins.
}
\details{
The conditioning variable is partitioned into bins using either
equal-width histogram bins or boxplot-style breaks. Within each bin,
the AreaPDF and Hellinger distance are computed between the two groups.
Bins with fewer than \code{min_per_group} observations in either group are
skipped.
}
\examples{
set.seed(1)
df <- data.frame(
  score = rnorm(200),
  age = runif(200, 18, 65),
  group = rep(c(0, 1), each = 100)
)

compute_AreaPDF_cond(
  df,
  target_col = "score",
  condition_col = "age",
  group_col = "group"
)

}
